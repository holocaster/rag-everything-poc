services:
  rag:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - backend/.env
    volumes:
      - rag_data:/app/backend/data
      - rag_chroma_db:/app/backend/chroma_db

volumes:
  rag_data:
  rag_chroma_db:
